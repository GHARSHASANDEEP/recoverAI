# app/

This directory is reserved for future modular Streamlit page components.

The current dashboard entry point is `app.py` at the repository root.

Run the dashboard with:

```powershell
.\.venv\Scripts\python.exe -m streamlit run app.py
```
